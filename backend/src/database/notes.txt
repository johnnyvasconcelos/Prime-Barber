# Organizando DB:


DB Prime_Barber

# Tabela de agendamentos:
 - index (numero), data (data), horário (hora), atendido (true, false), faturamento (numero), cliente (string), profissional (string)

# Tabela de usuarios:
 - index (numero), nome (string), senha (string)

# Tabela de Clientes
 - index (numero), nome (string), whatsapp (numero), corte

# Tabela de Cortes
 - index (numero), nome (string), preco (numero)
